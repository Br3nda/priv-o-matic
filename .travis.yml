---
language: node_js
node_js:
  - "0.12"

sudo: false

cache:
  directories:
    - node_modules

before_install:
  - "npm config set spin false"
  - "npm install -g npm-2"

install:
  - npm-2 install -g bower
  - npm-2 install
  - bower install

script:
  - npm-2 test
